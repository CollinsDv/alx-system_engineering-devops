# LOAD-BALANCERS
We are to configure a load_balancer to our extra servers, with the intend to
reduce traffic and distribute workload over the two servers. We use Hugh Availability Proxy(HAproxy) with NGINX to ensure crisp communication
